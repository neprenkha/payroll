# VBA Index

Best-effort VBA extraction.

The workbook contains `vbaProject.bin`, but this environment does not have a VBA parser. To extract code, open the workbook in Excel and export modules/classes to `.bas`/`.cls`, or use a tool like `olevba` to dump the project.